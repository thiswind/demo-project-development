{
  "name": "demo-project-development",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "DEMO technical validation project for local development, GitHub, GitHub Actions, GitHub Pages, Vercel deployment, and Vercel custom domain binding.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "docs:dev": "vitepress docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "check": "npm run build && npm run docs:build"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "vite": "latest",
    "vitepress": "latest"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "typescript": "~5.8.3"
  }
}
